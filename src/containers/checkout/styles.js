import { DangerButton } from 'sharedStyle'
import styled from 'styled-components'

export const CheckoutWrapper = styled.div`
  text-align: center;
  width: 100%;
  margin: auto;
`
export const MainButton = styled(DangerButton)`
  margin-top: 6rem;
`
